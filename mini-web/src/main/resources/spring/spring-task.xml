<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:task="http://www.springframework.org/schema/task"
       xsi:schemaLocation="
			http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.1.xsd
			http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.1.xsd
			http://www.springframework.org/schema/task http://www.springframework.org/schema/task/spring-task-4.1.xsd">

    <!-- 配置注解扫描 -->
    <context:component-scan base-package="com.gongsi.mini.task" annotation-config="true"/>

    <task:executor id="taskExecutor" pool-size="3"/>

    <task:scheduler id="taskScheduler" pool-size="10"/>

    <task:scheduled-tasks scheduler="taskScheduler">
        <!-- 股票同步,半个小时执行一次  -->
        <task:scheduled ref="stockTask" method="execute" cron="0 0/30 * * * * "/>

        <!-- 刷新apptoken,5分钟执行一次  -->
        <task:scheduled ref="appTokenTask" method="execute" cron="0 0/5 * * * * "/>

        <!-- 发送邮件,1分钟执行一次  -->
        <task:scheduled ref="sendEmailTask" method="execute" cron="0 0/1 * * * * "/>

    </task:scheduled-tasks>

</beans>